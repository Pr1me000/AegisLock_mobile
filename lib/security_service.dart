class SecurityService {
  static Future<void> sendEncryptedKey(String deviceId) async {
    print("Tentative d'échange sécurisé avec : $deviceId");
    // reste du code pour sécuriser l'échange de la clé ... @Uramix
  }
}